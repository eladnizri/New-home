# הפעלת סנכרון אמיתי

האפליקציה עובדת מקומית כמו שהיא. המדריך הזה מוסיף סנכרון חי בין מכשירים —
אתה מעדכן מחיר בטלפון בחנות, וזה מופיע מיד אצל בן/בת הזוג.

זמן משוער: רבע שעה.

---

## 1. פרויקט Firebase

1. היכנס ל־[console.firebase.google.com](https://console.firebase.google.com) וצור פרויקט.
   אפשר לכבות Google Analytics — לא צריך אותו כאן.
2. בתפריט: **Build → Firestore Database → Create database**.
   בחר **Production mode** (את הכללים נגדיר בשלב 3) ואזור אירופי, למשל `eur3`.
3. בתפריט: **Build → Authentication → Get started → Anonymous → Enable**.
   זה מה שמאפשר לאפליקציה לעבוד בלי מסך התחברות.

## 2. הקונפיג

1. גלגל ההגדרות (למעלה משמאל) → **Project settings**.
2. תחת **Your apps** לחץ על אייקון ה־Web (`</>`), תן שם כלשהו, **Register app**.
3. תקבל סניפט קוד. שני דברים ממנו:
   - אובייקט `firebaseConfig` — מעתיק **את כולו**
   - מספר הגרסה שמופיע בכתובות, למשל `https://www.gstatic.com/firebasejs/12.1.0/...`

בקובץ `index.html`, בתוך תגית ה־`<script>`, החלף:

```js
const FIREBASE_CONFIG = null;
const FIREBASE_SDK_VERSION = "";
```

ב:

```js
const FIREBASE_CONFIG = {
  apiKey: "...",
  authDomain: "....firebaseapp.com",
  projectId: "...",
  storageBucket: "....appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
const FIREBASE_SDK_VERSION = "12.1.0";   // המספר מהסניפט שלך
```

מפתח ה־API כאן הוא מזהה ציבורי ולא סוד — הוא אמור לשבת בקוד גלוי.
מה שמגן על המידע הוא הכללים בשלב הבא.

## 3. כללי אבטחה

**Firestore Database → Rules**, מחק את מה שיש, הדבק את התוכן של
[`firestore.rules`](./firestore.rules), ולחץ **Publish**.

הכללים אומרים: מי שמחובר ומחזיק במזהה בית תקין (24 תווים אקראיים) יכול לקרוא
ולכתוב את הבית הזה בלבד. כל השאר סגור.

## 4. פרסום ל־GitHub Pages

בריפו: **Settings → Pages**

- **Source**: Deploy from a branch
- **Branch**: `claude/home-shopping-planner-vxf1wn`, תיקייה `/ (root)`
- **Save**

אחרי דקה־שתיים האפליקציה תהיה ב־`https://eladnizri.github.io/New-home/`.

## 5. הרשאת הדומיין ב־Firebase

זה השלב שהכי קל לשכוח, ובלעדיו ההתחברות תיכשל.

**Authentication → Settings → Authorized domains → Add domain** → `eladnizri.github.io`

---

## שיתוף

בכניסה הראשונה האפליקציה מייצרת מזהה בית ומוסיפה אותו לכתובת:

```
https://eladnizri.github.io/New-home/#h=k7f2p9x4m1n8q3
```

**הקישור הזה הוא ההרשאה.** כפתור "העתקת קישור לשיתוף" בתחתית העמוד מעתיק אותו.
מי שנכנס דרכו רואה ועורך את אותו מידע בזמן אמת. מי שנכנס בלעדיו יקבל בית חדש וריק.

שמור את הקישור. אין דרך לשחזר מזהה בית שאבד.

## חיווי הסנכרון

בכותרת יש נורית מצב:

| מצב | פירוש |
|---|---|
| מסונכרן | הכל מעודכן אצל שניכם |
| שומר… | השינוי בדרך לענן |
| לא מקוון | אין רשת. השינויים נשמרים מקומית ויסתנכרנו לבד כשהקליטה תחזור |
| מקומי בלבד | לא הוגדר קונפיג — הנתונים בדפדפן הזה בלבד |
| הסנכרון נכשל | ההתחברות נכשלה. האפליקציה ממשיכה לעבוד מקומית |

## איך זה בנוי

כל פריט הוא מסמך נפרד ב־`homes/{homeId}/items/{itemId}`, והתקציב ותאריך הכניסה
יושבים על `homes/{homeId}`. זה מכוון: אם אתה עורך את "ספה" ובן/בת הזוג עורכים
את "מקרר" באותו רגע, שני השינויים נשמרים. ברזולוציה של קובץ אחד גדול, אחד מהם
היה נדרס.

מזהי פריטי ברירת המחדל קבועים, כך ששני מכשירים שנכנסים לקישור טרי באותו זמן
מתכנסים לאותם מסמכים במקום לייצר כפילויות.

## עלות

ה־free tier של Firebase (תוכנית Spark) נותן 50 אלף קריאות ו־20 אלף כתיבות ליום.
שני משתמשים ומאה פריטים לא מתקרבים לזה. אין צורך בכרטיס אשראי.

## אם משהו לא עובד

**הנורית תקועה על "הסנכרון נכשל"** — פתח את הקונסולה בדפדפן. אם השגיאה על טעינת
ה־SDK, מספר הגרסה ב־`FIREBASE_SDK_VERSION` שגוי; קח אותו מהסניפט בקונסולת Firebase.
אם השגיאה `auth/unauthorized-domain`, חזור לשלב 5.

**"השינוי לא נשמר בענן: permission-denied"** — הכללים משלב 3 לא פורסמו.

**נכנסת ורואה רשימה ריקה או ברירת מחדל** — כנראה נכנסת בלי ה־`#h=...` בכתובת.
