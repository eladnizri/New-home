# הפעלת סנכרון אמיתי — שלב אחר שלב

האפליקציה עובדת מקומית כמו שהיא. המדריך הזה מוסיף סנכרון חי בין מכשירים —
אתה מעדכן מחיר בטלפון בחנות, וזה מופיע מיד אצל בן/בת הזוג.

**לפני שמתחילים:** חשבון גוגל, גישה לריפו ב־GitHub, וכ־15 דקות.
לא צריך כרטיס אשראי ולא צריך להתקין כלום.

> הערה: הניסוחים בקונסולה של Firebase משתנים מדי פעם. אם כפתור לא נקרא בדיוק
> כמו שכתוב כאן, חפש את הכפתור שעושה את אותו דבר — הרצף עצמו לא משתנה.

---

## שלב 1 · יצירת פרויקט Firebase

1. היכנס ל־[console.firebase.google.com](https://console.firebase.google.com) והתחבר עם חשבון גוגל.
2. **Create a project**.
3. שם הפרויקט: `new-home`. מתחתיו ייווצר מזהה ייחודי כמו `new-home-a1b2c` — זה תקין.
4. במסך Google Analytics — **כבה** את המתג. אין בו צורך כאן והוא רק מוסיף שלבים.
5. **Create project**, המתן כחצי דקה, ואז **Continue**.

## שלב 2 · הפעלת Firestore

1. בתפריט הצדדי: **Build → Firestore Database**.
2. **Create database**.
3. **בחר אזור לפני היצירה** — למשל `eur3 (europe-west)`.
   את האזור אי אפשר לשנות אחר כך בלי למחוק את מסד הנתונים.
4. במסך הכללים בחר **Production mode**.
   אל תבחר Test mode — הוא פותח את המסד לכל אחד למשך 30 יום, ובשלב 6 ממילא
   נגדיר כללים אמיתיים.
5. **Create**.

## שלב 3 · הפעלת התחברות אנונימית

זה מה שמאפשר לאפליקציה לעבוד בלי מסך התחברות.

1. **Build → Authentication → Get started**.
2. לשונית **Sign-in method**.
3. ברשימת הספקים בחר **Anonymous**, הפעל את המתג, **Save**.

## שלב 4 · רישום האפליקציה וקבלת הקונפיג

1. לחץ על גלגל השיניים ליד **Project Overview** → **Project settings**.
2. גלול מטה ל־**Your apps** ולחץ על אייקון ה־Web: **`</>`**.
3. App nickname: `new-home`.
   **אל תסמן** את "Also set up Firebase Hosting" — אנחנו מארחים ב־GitHub Pages.
4. **Register app**.
5. יוצג לך קוד. שמור ממנו **שני** דברים:
   - את האובייקט `firebaseConfig` במלואו
   - את **מספר הגרסה** מתוך הכתובת, למשל `12.1.0` מתוך
     `https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js`
6. **Continue to console**.

## שלב 5 · הדבקת הקונפיג בקוד

הכי נוח לערוך ישירות בדפדפן:

1. פתח את [`index.html`](https://github.com/eladnizri/New-home/blob/claude/home-shopping-planner-vxf1wn/index.html) בריפו.
2. לחץ על אייקון העיפרון (**Edit this file**).
3. גלול ל**שורה 801**. שם כתוב:

```js
const FIREBASE_CONFIG = null;
```

4. החלף אותה ואת **שורה 804** כך:

```js
const FIREBASE_CONFIG = {
  apiKey: "AIza...",
  authDomain: "new-home-a1b2c.firebaseapp.com",
  projectId: "new-home-a1b2c",
  storageBucket: "new-home-a1b2c.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
```

```js
const FIREBASE_SDK_VERSION = "12.1.0";
```

הערכים למעלה הם דוגמה — הדבק את שלך. שים לב לנקודה־פסיק בסוף האובייקט.

5. **Commit changes** → **Commit directly to the `claude/home-shopping-planner-vxf1wn` branch**.

מפתח ה־API כאן הוא מזהה ציבורי ולא סוד, ולכן מותר לו לשבת בריפו גלוי.
מה שמגן על המידע הוא הכללים בשלב הבא.

## שלב 6 · כללי אבטחה

1. חזור ל־**Firestore Database** → לשונית **Rules**.
2. מחק את כל מה שכתוב שם.
3. הדבק את התוכן המלא של [`firestore.rules`](./firestore.rules).
4. **Publish**.

הכללים אומרים: מי שמחובר ומחזיק במזהה בית תקין יכול לקרוא ולכתוב את הבית הזה
בלבד. כל השאר סגור.

## שלב 7 · פרסום ל־GitHub Pages

1. בריפו: **Settings** (בסרגל העליון) → **Pages** (בתפריט הצדדי).
2. **Source**: `Deploy from a branch`.
3. **Branch**: `claude/home-shopping-planner-vxf1wn`, תיקייה `/ (root)`.
4. **Save**.
5. המתן דקה־שתיים ורענן. בראש העמוד תופיע הכתובת:
   `https://eladnizri.github.io/New-home/`

## שלב 8 · הרשאת הדומיין ב־Firebase

**זה השלב שהכי קל לשכוח, ובלעדיו ההתחברות תיכשל.**

1. **Authentication → Settings** (לשונית) → **Authorized domains**.
2. **Add domain** → הקלד `eladnizri.github.io` → **Add**.

## שלב 9 · בדיקה

1. פתח את `https://eladnizri.github.io/New-home/`.
2. הכתובת בשורת הכתובת תתארך ותקבל סיומת כמו `#h=k7f2p9x4m1n8q3` — זה מזהה הבית
   שנוצר לך. **שמור את הכתובת המלאה הזאת.**
3. הנורית בכותרת, ליד "סנכרון", צריכה להגיע ל**מסונכרן** (עיגול ירוק).
4. שנה משהו — למשל מחיר של ספה בסלון.
5. לחץ **העתקת קישור לשיתוף** בתחתית העמוד, ופתח את הקישור בטלפון או בדפדפן אחר.
   אתה אמור לראות את אותו שינוי.

זהו. מכאן כל שינוי אצל אחד מכם מופיע אצל השני תוך שנייה.

---

## שיתוף

**הקישור עם `#h=...` הוא ההרשאה**, בדיוק כמו לינק לגוגל דוקס שלא רשום בשום מקום.
מי שנכנס דרכו רואה ועורך את המידע שלכם. מי שנכנס בלי הסיומת יקבל בית חדש וריק.

אין דרך לשחזר מזהה בית שאבד. שמור את הקישור במקום קבוע — למשל הודעה נעוצה
בצ׳אט ביניכם.

## חיווי הסנכרון

| מצב | פירוש |
|---|---|
| מסונכרן | הכל מעודכן אצל שניכם |
| שומר… | השינוי בדרך לענן |
| לא מקוון | אין רשת. השינויים נשמרים מקומית ויסתנכרנו לבד כשהקליטה תחזור |
| מקומי בלבד | לא הוגדר קונפיג — הנתונים בדפדפן הזה בלבד |
| הסנכרון נכשל | ההתחברות נכשלה. האפליקציה ממשיכה לעבוד מקומית |

## אם משהו לא עובד

| מה שאתה רואה | מה לעשות |
|---|---|
| הנורית תקועה על **מקומי בלבד** | הקונפיג לא נתפס. ודא ששורה 801 מתחילה ב־`const FIREBASE_CONFIG = {` ולא `= null`, ושה־commit באמת נשמר בברנץ' |
| **הסנכרון נכשל**, ובקונסולת הדפדפן שגיאה על טעינת SDK | מספר הגרסה שגוי. קח אותו מהסניפט בשלב 4 |
| **הסנכרון נכשל**, בקונסולה `auth/unauthorized-domain` | שלב 8 לא בוצע |
| **השינוי לא נשמר בענן: permission-denied** | הכללים משלב 6 לא פורסמו |
| GitHub Pages מחזיר 404 | חכה עוד 2־3 דקות. ודא שבשלב 7 בחרת `/ (root)` ולא `/docs` |
| נכנסת ורואה רשימת ברירת מחדל במקום שלכם | נכנסת בלי ה־`#h=...` בכתובת |

לפתיחת קונסולת הדפדפן: `F12` במחשב, לשונית Console.

## איך זה בנוי

כל פריט הוא מסמך נפרד ב־`homes/{homeId}/items/{itemId}`, והתקציב ותאריך הכניסה
יושבים על `homes/{homeId}`. זה מכוון: אם אתה עורך את "ספה" ובן/בת הזוג עורכים
את "מקרר" באותו רגע, שני השינויים נשמרים. ברזולוציה של קובץ אחד גדול, אחד מהם
היה נדרס.

מזהי פריטי ברירת המחדל קבועים, כך ששני מכשירים שנכנסים לקישור טרי באותו זמן
מתכנסים לאותם מסמכים במקום לייצר כפילויות.

## עלות

ה־free tier של Firebase (תוכנית Spark) נותן 50 אלף קריאות ו־20 אלף כתיבות ליום.
שני משתמשים ומאה פריטים לא מתקרבים לזה.
